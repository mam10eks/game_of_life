{
  "name": "game_of_life",
  "version": "1.0.0",
  "description": "An elm implementation of Conway's Game of Life",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "babel src --presets env --out-dir build && browserify build/*.js > build/all.js && find build/*.js -not -name all.js -delete && rm -Rf build/generated",
    "start": "./build_scripts/run_in_docker_container.sh"
  },
  "repository": {
    "type": "git",
    "url": "gitlab@gitlab.empty-stack.com:maik/game_of_life.git"
  },
  "author": "Maik Fröbe",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.6.0",
    "browserify": "^14.4.0",
    "elm": "^0.18.0",
    "elm-format": "^0.6.1-alpha",
    "js-cookie": "^2.1.4"
  }
}
